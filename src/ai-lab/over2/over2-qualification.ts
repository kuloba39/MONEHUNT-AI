/**
 * OVER 2 QUALIFICATION
 *
 * Compatibility module for the dedicated OVER 2 engine.
 *
 * The actual qualification logic lives in the shared
 * Matches digit engine so both systems use the exact
 * same rules.
 */

export {
    evaluateOver2Signal,
} from '../matches/digit-engine';
